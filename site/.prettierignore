dist/
.astro/
pnpm-lock.yaml
public/llms.txt
public/llms-full.txt
# .astro components are handled by `astro check`/eslint, not prettier
# (no prettier-plugin-astro installed).
*.astro

# The evaluator results pages are generated by cmd/gen_eval_pages. Prettier
# reflows their tables and mangles the content while doing it — `next_steps`
# becomes `next*steps`, `_inside_` becomes `\_inside*` — because it re-parses cell
# text as emphasis. Their tables are the generator's to own.
src/content/docs/eval-results.mdx
src/content/docs/es/eval-results.mdx
