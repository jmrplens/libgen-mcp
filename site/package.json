{
  "name": "libgen-mcp-site",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/copy-llms.mjs && node scripts/sync-privacy.mjs",
    "build": "astro build",
    "postbuild": "node scripts/normalize-dist-html.mjs && node scripts/check-dist-html.mjs && node scripts/add-sitemap-lastmod.mjs && node scripts/emit-page-markdown.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "i18n:check": "node scripts/check-i18n-parity.mjs",
    "privacy:check": "node scripts/sync-privacy.mjs --check",
    "contrast:check": "node scripts/check-contrast.mjs",
    "facts:check": "node scripts/check-facts.mjs",
    "faq:check": "node scripts/gen-faq-schema.mjs --check",
    "eslint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "html-validate": "html-validate 'dist/**/*.html'",
    "htmlhint": "htmlhint 'dist/**/*.html'",
    "pa11y": "astro preview & sleep 3 && pa11y-ci; EXIT=$?; kill %1 2>/dev/null; exit $EXIT",
    "test:a11y": "pnpm run pa11y",
    "lint": "pnpm run check && pnpm run i18n:check && pnpm run privacy:check && pnpm run contrast:check && pnpm run facts:check && pnpm run faq:check && pnpm run eslint && pnpm run format:check && pnpm run html-validate && pnpm run htmlhint",
    "analyze": "pnpm run build && pnpm run lint"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.3.1",
    "@astrojs/starlight": "^0.42.2",
    "astro": "^7.3.3",
    "mermaid": "^12.0.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@typescript-eslint/eslint-plugin": "^8.70.1",
    "@typescript-eslint/parser": "^8.70.1",
    "eslint": "^10.11.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-astro": "^3.2.1",
    "html-validate": "^11.16.0",
    "htmlhint": "^1.9.2",
    "js-yaml": "4.3.1",
    "pa11y-ci": "^4.1.1",
    "playwright": "^1.63.0",
    "prettier": "^3.9.8",
    "rehype-mermaid": "^3.0.0",
    "serve": "^14.2.6",
    "starlight-links-validator": "^0.26.0",
    "typescript": "^6.0.3"
  }
}
