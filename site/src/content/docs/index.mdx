---
title: LibGen MCP
description: "Open-source MCP server in Go for Library Genesis: four tools to search, download, and read books, papers and more from your AI assistant — no account required."
head:
  - tag: title
    content: "LibGen MCP — MCP server for Library Genesis book & paper search"
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "inLanguage": "en",
        "isPartOf": { "@id": "https://jmrplens.github.io/libgen-mcp/#website" },
        "about": { "@id": "https://github.com/jmrplens/libgen-mcp#software" },
        "mainEntity": [
          {
            "@type": "Question",
            "name": "What is libgen-mcp?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "libgen-mcp is an open source Model Context Protocol (MCP) server, written in Go, for searching and downloading from Library Genesis (the libgen.li mirror family). It exposes four tools — search, get_details, download, and read — to any MCP-compatible client such as Claude Code, Claude Desktop, or your own agent, so you can search books, resolve download links, and read/summarize text in plain language."
            }
          },
          {
            "@type": "Question",
            "name": "What can libgen-mcp do?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "It searches the Library Genesis catalog, returns per-result metadata and MD5 hashes, fetches full details for a record, and downloads files to a local directory. Books are downloaded by MD5 from LibGen, and articles are fetched by DOI from open-access (Unpaywall) and Sci-Hub sources. The search tool can additionally, opt-in, federate keyless open-access discovery from arXiv, Crossref, and OpenLibrary, merged and deduped alongside the Library Genesis results. Mirrors are discovered automatically and cached, with transparent failover, retry with cooldown, and a shared rate limiter, so the server keeps working as individual mirrors go up and down."
            }
          },
          {
            "@type": "Question",
            "name": "Do I need an account, API key, or token to use libgen-mcp?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "No. libgen-mcp requires no Library Genesis account, no API key, and no token. Every option is an environment variable with a documented default, so the server runs with zero configuration. The only email-shaped setting is the optional LIBGEN_MCP_UNPAYWALL_EMAIL contact address sent to the Unpaywall API for DOI lookups. It is unset by default, which disables the Unpaywall source (Sci-Hub still serves DOIs without it); set your own address to enable Unpaywall."
            }
          },
          {
            "@type": "Question",
            "name": "Is libgen-mcp free and open source?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. libgen-mcp is free and open source under the MIT license. The full source code is on GitHub at https://github.com/jmrplens/libgen-mcp, and prebuilt binaries, a Docker image, and a Claude Desktop extension are published with each release."
            }
          }
        ]
      }
hero:
  tagline: A Go MCP server with four tools — search, get_details, download, read — that lets your AI assistant search Library Genesis, resolve download links, and read/summarize text in plain language. No account or API key.
  image:
    dark: ../../assets/logo-dark.svg
    light: ../../assets/logo-light.svg
  actions:
    - text: Getting Started
      link: /libgen-mcp/getting-started/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/jmrplens/libgen-mcp
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Aside } from "@astrojs/starlight/components";

**LibGen MCP** is an [MCP](https://modelcontextprotocol.io) server, written in Go, for searching and downloading from **Library Genesis** (the `libgen.li` mirror family). It exposes four tools — `search`, `get_details`, `download`, and `read` — to any MCP-compatible client such as Claude Code, Claude Desktop, or your own agent.

Mirrors are discovered automatically and cached, with transparent failover, so the server keeps working as individual mirrors go up and down. Articles can also be fetched from open-access and Sci-Hub sources by DOI. `search` can additionally, opt-in, federate keyless open-access discovery from arXiv, Crossref, and OpenLibrary — merged, deduped, and labeled by origin alongside the usual Library Genesis results.

<CardGrid>
	<Card title="Four tools" icon="puzzle">
		`search`, `get_details`, `download`, and `read` — read-only search,
		metadata, and paginated text extraction plus a non-destructive, resumable
		download.
	</Card>
	<Card title="Resilient mirrors" icon="random">
		Automatic mirror discovery, failover, retry with cooldown, and a shared rate
		limiter.
	</Card>
	<Card title="Books and articles" icon="open-book">
		Books by MD5 from LibGen; articles by DOI from Unpaywall and Sci-Hub.
	</Card>
	<Card title="Open-access discovery" icon="magnifier">
		`search` can opt in to keyless discovery from arXiv, Crossref, and
		OpenLibrary — no account or API key needed.
	</Card>
	<Card title="Configured by env" icon="setting">
		Every option is an environment variable with a documented default and
		fail-fast validation.
	</Card>
</CardGrid>

## Start here

<LinkCard
	title="Getting Started"
	description="Install (prebuilt binary, Docker, or go install), wire the server into an MCP client, and run your first search."
	href="/libgen-mcp/getting-started/"
/>
<LinkCard
	title="Tools"
	description="The search, get_details, download, and read tools — inputs, outputs, and error behavior."
	href="/libgen-mcp/tools/"
/>
<LinkCard
	title="Configuration"
	description="Every environment variable, with its default, valid range, and meaning."
	href="/libgen-mcp/configuration/"
/>

## Responsible use

<Aside type="caution">
	This tool accesses third-party mirrors of Library Genesis. You are responsible
	for respecting the copyright and intellectual-property laws that apply where
	you live. Use it only for content you are legally entitled to access.
</Aside>

## Frequently asked questions

### What is libgen-mcp?

libgen-mcp is an open source Model Context Protocol (MCP) server, written in Go, for searching and downloading from Library Genesis (the `libgen.li` mirror family). It exposes four tools — `search`, `get_details`, `download`, and `read` — to any MCP-compatible client such as Claude Code, Claude Desktop, or your own agent, so you can search books, resolve download links, and read/summarize text in plain language.

### What can libgen-mcp do?

It searches the Library Genesis catalog, returns per-result metadata and MD5 hashes, fetches full details for a record, and downloads files to a local directory. Books are downloaded by MD5 from LibGen, and articles are fetched by DOI from open-access (Unpaywall) and Sci-Hub sources. The `search` tool can additionally, opt-in, federate keyless open-access discovery from arXiv, Crossref, and OpenLibrary, merged and deduped alongside the Library Genesis results. Mirrors are discovered automatically and cached, with transparent failover, retry with cooldown, and a shared rate limiter, so the server keeps working as individual mirrors go up and down.

### Do I need an account, API key, or token to use libgen-mcp?

No. libgen-mcp requires no Library Genesis account, no API key, and no token. Every option is an environment variable with a documented default, so the server runs with zero configuration. The only email-shaped setting is the optional `LIBGEN_MCP_UNPAYWALL_EMAIL` contact address sent to the Unpaywall API for DOI lookups. It is unset by default, which disables the Unpaywall source (Sci-Hub still serves DOIs without it); set your own address to enable Unpaywall.

### Is libgen-mcp free and open source?

Yes. libgen-mcp is free and open source under the MIT license. The full source code is on GitHub at [github.com/jmrplens/libgen-mcp](https://github.com/jmrplens/libgen-mcp), and prebuilt binaries, a Docker image, and a Claude Desktop extension are published with each release.
